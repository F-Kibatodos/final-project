module.exports = categoryFilter => {
  if (categoryFilter === '1') return (categoryFilter = 'dollor')
  else if (categoryFilter === '2') return (categoryFilter = 'autem')
  else if (categoryFilter === '3') return (categoryFilter = 'nihil')
}
